name: Deploy apps to GitHub Pages

on:
  push:
    branches:
      - main
      - claude/she-comes-first-app-q3o95v
      - claude/iphone-christianity-app-54ri1n
    paths:
      - "she-comes-first/**"
      - "foundations/**"
      - ".github/workflows/deploy-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Assemble site
        run: |
          mkdir -p dist
          cp -r she-comes-first/. dist/
          cp -r foundations dist/foundations

      - name: Upload site
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
